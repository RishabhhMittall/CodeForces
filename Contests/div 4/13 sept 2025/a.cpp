#include<bits/stdc++.h>
using namespace std;

typedef long long ll;

int main() {

    int t;
    cin >> t;

    while(t--) {
        // int n;
        // cin >> n;

        // vector<int> arr(n);

        int a , b;
        cin >> a >> b;

        if(b % 2 == 0) {
            cout << 0 << endl;
        } else {
            cout << a << endl;
        }

        
    }

    return 0;
}